# Client environment variables
# Copy to client/.env — optional for local development, since the app already
# falls back to http://localhost:3001 when this is not set.

# Where the API and websocket server is running.
VITE_API_URL=http://localhost:3001
