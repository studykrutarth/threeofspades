node_modules
# Editor and local AI-tool configuration
.claude/
.vscode/
# Keep environment variables out of version control
.env

/generated/prisma

# Local training state for the bot trainer. The weights it produces
# (bot-params.json) are committed; the population it is mid-run on, and the
# archives it makes of superseded metrics, are not.
server/engine/evolution-checkpoint.json
server/engine/bot-params.*.json
